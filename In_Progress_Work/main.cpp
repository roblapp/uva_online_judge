#include <iostream>
#include <vector>
#include <fstream>

using namespace std;

//#define read cin
#define MAXN 26

int main() {
    ifstream read("test.txt");
    int ncases;
    vector<int> vertices[MAXN];

    read >> ncases;
    for (int i = 0; i < ncases; i++) {

    }
    read.close();
    return 0;
}
